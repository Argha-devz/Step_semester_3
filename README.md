# Step Semester 3 - Course Progress

## Student Information
- **Name:** Argha Banerjee
- **Registration Number:** RA2511003011874
- **Program:** B.Tech Computer Science and Engineering Core

---

## Progress Log

### Session 5: Arrays, Methods, and Object-Oriented Fundamentals
- **Date:** 29-08-2026
- **Branch:** `feature/session_5`
- **Completed Work:**
      
     - Initialized `feature/session_5` branch from develop.
  
     - Implemented Category C Practice problems under `session5/class_problems`:
        - `ScoreCurveBooster.java` (In-place array score manipulation and `Arrays.toString()` formatting)
        - `TeamNameFinder.java` (Nested loop comparison and duplicate string detection)
        - `PodiumFinder.java` (Single-pass top-3 maximum tracking without sorting)
        - `SeatingGridOptimizer.java` (Private helper method for jagged 2D array row averaging and zone classification)
        - `PlacementEngine.java` (Method overloading, `Comparable` implementation, encapsulation, and `Arrays.sort()` ranking)

     - Implemented Category C Assignment problems under `session5/assignment_problems`:
       - `FantasyScoreMultiplier.java` (Direct array score updating via reference for captains and vice-captains)
       - `DuplicatePlayerChecker.java` (Nested loop pairwise comparison for roster verification)
       - `TopPerformerTracker.java` (Single-pass tracking of min, max, and spread without sorting)
       - `MatchDayGridAnalyzer.java` (Private row-averaging helper method for jagged 2D grid categorization)
       - `AutoDraftEngine.java` (Overloaded draft rules, Comparable interface implementation, and standard library sorting)

### Session 4: Arrays, Loops, and Conditionals
- **Date:** 29-08-2026
- **Branch:** `feature/session_4`
- **Completed Work:**

     - Initialized `feature/session_4` branch from develop.

     - Implemented Category C Class problems under `week4_arrays/class_problems`:
        - `TwoSum.java` (Nested for loops and pairwise search logic)
        - `BestTimeToBuyAndSellStock.java` (Single-pass array traversal and running minimum tracking)
        - `ContainsDuplicate.java` (Nested loops and pairwise comparison logic)
        - `MergeSortedArrays.java` (Two-pointer array traversal and while loops)
        - `RotateArray.java` (Modulo arithmetic for wraparound indexing)

     - Implemented Category C Assignment problems under `week4_arrays/assignment_problems`:
        - `ProductExceptSelf.java` (Prefix and suffix products with two-pass traversal)
        - `MaximumSubarray.java` (Kadane's algorithm and running-sum reset logic)
        - `ThreeSum.java` (Sorting, two-pointer technique, and duplicate avoidance)
        - `SubarraySumEqualsK.java` (Prefix sums and hash map frequency counting)
        - `MinInRotatedArray.java` (Modified binary search on a rotated sorted array)

### Session 3: Control Flow & Conditionals
- **Date:** 24-08-2026
- **Branch:** `feature/session_3`
- **Completed Work:**
    
     - Initialized `feature/session_3` branch from develop.
       
     - Implemented Category C Assignment problems under `control_flow/assigment_problems`:
        - `VotingChecker.java` (Boolean expressions and if/else)
        - `NumberClassifier.java` (Three-way decision ladder)
        - `NumberPrinter.java` (For loops and iteration counters)
        - `NaturalSum.java` (While loops and accumulator variables)
        - `DayNamePrinter.java` (Switch statements, cases, and default validation)

### Session 2: Advanced Strings & Applications
- **Date:** 2026-03
- **Branch:** `feature/session_2`
- **Completed Work:**
     
     - Initialized `feature/session_2` branch from develop.

     - Implemented Category C Class/Practice problems under `strings_advanced/class_problems`:
        - `VowelConsonantCounter.java` (Character iteration using `charAt()` and case-insensitive vowel/consonant validation)
        - `CsvRecordParser.java` (CSV splitting, array length checks, and formatted string output)
        - `FileExtensionValidator.java` (Extension extraction using `lastIndexOf()` and `equalsIgnoreCase()`)
        - `MaskedPhoneFormatter.java` (Length validation, `StringBuilder` manipulation, and masking patterns)
        - `TransactionReferenceManager.java` (Multi-stage string normalization, `Character.isLetter()`/`isDigit()` checks, and formatting)

     - Implemented Category C Assignment problems under `strings_advanced/assignment_problems`:
        - `PinLengthValidator.java` (`length()` check and basic conditional flow)
        - `WordReversalEncoder.java` (String splitting, `StringBuilder` reversal, and word-order preservation)
        - `ProductInventoryParser.java` (CSV line parsing, field count validation, and record formatting)
        - `LibraryIsbnManager.java` (String trimming, uppercase normalization, and character-type validation loops)
        - `StopWordFilteredReport.java` (Text normalization, `replace()`, stop-word filtering, and frequency sorting)

### Session 1: Basic Strings & Fundamentals
- **Date:** 2026-03
- **Branch:** `feature/session_1`
- **Completed Work:**
     
     - Initialized `feature/session_1` branch from develop.

     - Implemented Category C Class problems under `string/class_problems`:
       - `RockPaperScissors.java` (Random number generation, conditional logic, and round outcome summary)
       - `PalindromeChecker.java` (Iterative, recursive, and array-reversal string checks)
       - `BmiCalculator.java` (Parallel array processing, BMI calculation, and status classification)
       - `FirstNonRepeatingChar.java` (Character frequency tracking and early-exit scanning)
       - `ReverseCustomerName.java` (String traversal and character array reversal)

     - Implemented Category C Assignment problems under `string/assignment_problems`:
       - `SeatDuplicateChecker.java` (Array scanning and duplicate seat number validation)
       - `TypingAccuracyChecker.java` (Character-by-character string comparison and accuracy percentage)
       - `TrafficStreakAnalyzer.java` (String traversal and tracking running character streaks)
       - `WarehouseInventoryBalancer.java` (Section quantity sum comparison and maximum tracking)
       - `WordLengthProfiler.java` (String splitting, length categorization, and frequency counting)
  